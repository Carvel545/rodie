public class Main {
    public static void main(String[] args)
        String name = scanner.next();
        System.out.println("Привет, " + name.substring(0, 1).toUpperCase() + name.substring(1).toLowerCase() + "!");
   }
}
